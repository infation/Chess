package ramapo.edu.sminev.chess.Model;

import java.util.Vector;

/**
 * Created by sminev on 2/22/18.
 */

public class Rules {
    public Rules(){

    }
/*
    public static Vector<Coords> refineAvailableMoves(Player player, Vector<Coords> moves, int turn){
        for(int i = 0; i < moves.size(); i++){
            for(int j = 0; j < player.getPieces().size(); j++){
                if(player.getPieces().get(j).getCoords().isEqual(moves.get(i))){
                    moves.remove(j);
                }
            }
        }
        for(int i = 0; i < moves.size();i++){
            for(int)
        }
    }*/
}
